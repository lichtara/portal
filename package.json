{
  "name": "portal",
  "private": true,
  "workspaces": [
    "apps/app-web"
  ],
  "scripts": {
    "build": "npm run build -w apps/app-web",
    "lint": "npm run lint -w apps/app-web",
    "format": "npm run format -w apps/app-web",
    "format:check": "npm run format:check -w apps/app-web",
    "test": "echo test placeholder"
  }
}
