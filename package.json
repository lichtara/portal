{
  "name": "portal",
  "private": true,
  "workspaces": [
    "apps/app-web"
  ],
  "scripts": {
    "build": "npm run build -w apps/app-web",
    "lint": "echo lint placeholder",
    "test": "echo test placeholder"
  }
}

